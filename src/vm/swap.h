//this should just include methods in which we

//make bitmap
//add elem to bit map
//remove elem from bit map
//find elem in bit map
#include "vm/page.h"
void swap_init (void);
void swap_insert (struct spt_entry *);
void swap_get (struct spt_entry *);
void swap_free (struct spt_entry *);
